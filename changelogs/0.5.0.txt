- move 'sets' to a new list (instead of the map) named 'variants'
- 'set' is now a key inside the variants
note: the format will change in 0.5.0 to something even more flexible

Missing :
- Into the Inklands needs the updated list of actions